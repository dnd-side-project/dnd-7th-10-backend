version: '3'

services:
  spring:
    image: audtncks2/spring-app
    ports:
      - "8080:8080"
  nginx:
    restart: always
    image: audtncks2/nginx
    ports:
      - "80:80"
    depends_on:
      - spring
